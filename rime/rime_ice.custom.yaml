patch:
  reduce_english_filter/mode: all
  engine/processors:
      - lua_processor@select_character          # 以词定字
      # - ascii_composer
      - recognizer
      - key_binder
      - speller
      - punctuator
      - selector
      - navigator
      - express_editor
